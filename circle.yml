machine:
  services:
    - docker
    - mongodb
  node:
    version: v5.6.0

dependencies:
  override:
    - npm install
    - npm test
    - docker info
    - docker build -t echion/tasks .

general:
  artifacts:
    - ./coverage